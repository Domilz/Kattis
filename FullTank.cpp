#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <string>
#include <climits>
#include <sstream>
#include <cmath> 
#include <set>

void run() {
	int n, k;
	std::cin >> n >> k;
	std::vector<int> v;
	v.reserve(n);
	for (int i = 0; i < n; i++) {
		std::cout << v[i];
	}
	
}
 
 
int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(NULL);

	run();
}
